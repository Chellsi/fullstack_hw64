<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="<%= theme %>-theme">
  <header class="header">
    <nav class="nav">
      <div class="container">
        <div class="nav-brand">
          <a href="/">Express API</a>
        </div>
        <div class="nav-links">
          <a href="/users/page">Користувачі</a>
          <a href="/articles/page">Статті</a>
          <% if (user) { %>
            <a href="/auth/profile">
              <span class="user-avatar-small"><%= user.username.charAt(0).toUpperCase() %></span>
              <%= user.username %>
            </a>
          <% } else { %>
            <a href="/auth/login">Вхід</a>
            <a class="btn btn-small btn-primary" href="/auth/register">Реєстрація</a>
          <% } %>
        </div>
      </div>
    </nav>
  </header>
  <main class="main">
    <div class="container">